<div id="messages">
  <% flash.each do |name, message| %>
    <div class="alert alert-<%= change_for_bootstrap(name) %>">
      <button class="close" data-dismiss="alert" aria-label="close">
	&times;
      </button>
      <%= message %>
    </div>
  <% end %>
</div>
