<%= link_to coordinators_surveys_path(user: user.login), class: 'btn btn-primary show_surveys', title: "Опросы" do %>
  <span class="glyphicon glyphicon-list-alt"></span>
  <span class="badge">
    <%= user.surveys.count %>
  </span>
<% end %>
