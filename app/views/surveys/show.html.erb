<h1 class="header">Result of the survey from <%= @survey.pass_date %></h1>

<%= render 'survey', survey: @survey %>

<div id="agreement">
  <%= render 'surveys/agreement' %>
</div>

<div id="email_field">
  <%= render 'surveys/email_form' %>
</div>
