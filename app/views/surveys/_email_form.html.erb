<div class="panel panel-default">
  <div class="panel-heading">
    <p><strong>Если Вы хотите получить краткий отчет о результатах опроса, оставьте адрес электронной почты, на который его нужно будет направить.</strong></p>
  </div>
  <div class="panel-body">
    <%= form_for @survey, remote: true do |f| %>
      <div class="input-group">
	<%= f.email_field :user_email, class: 'form-control', id: 'survey_user_email', required: 'required' %>
	<span class="input-group-btn">
	  <%= f.submit "Оставить почту", class: 'btn btn-default', id: 'leave_email' %>
	</span>
      </div>
    <% end %>
  </div>
</div>
